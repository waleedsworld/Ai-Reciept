# Copy this file to .env and fill in your key.
# Required only for the AI features (receipt parsing, advice, chat).
OPENAI_API_KEY=your_openai_api_key_here

# Optional: change the port the dev server binds to (defaults to 5000).
# PORT=5000
